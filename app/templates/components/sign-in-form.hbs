{{email-input email=credentials.email}}
{{password-input password=credentials.password}}
<div class="form-group">
  <button type="submit" class="btn btn-primary" {{action "submit"}}>Sign In</button>
  <button class="btn btn-default" {{action "reset"}}>Cancel</button>
</div>
